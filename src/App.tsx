import AppRouter from "./routes/AppRouter";

/**
 * App
 *
 * Es el componente raíz de la aplicación.
 * Su única responsabilidad es cargar el sistema de rutas.
 */

export default function App() {
  return <AppRouter />;
}